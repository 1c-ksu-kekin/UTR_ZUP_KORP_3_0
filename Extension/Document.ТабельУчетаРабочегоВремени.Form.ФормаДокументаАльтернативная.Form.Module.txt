
&НаСервере
Процедура ЮТР_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	УстановитьВыполнениеПослеОбработчиковСобытия("ЮТР_ПриСозданииНаСервереПосле", Неопределено);		
	
КонецПроцедуры

&НаСервере
Процедура ЮТР_ПриСозданииНаСервереПосле(Отказ, СтандартнаяОбработка, ДополнительныйПараметр)
	
	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
	Элементы,
	"ЮТР_СпециалистОТИЗГруппа",
	"Видимость",
	(Объект.ЮТР_ДокументУтвержден ИЛИ ЮТР_ПользователиСлужебный.ЭтоСпециалистОтиз()));

	ОбщегоНазначенияКлиентСервер.УстановитьСвойствоЭлементаФормы(
	Элементы,
	"ЮТР_СпециалистОТИЗГруппа",
	"Доступность",
	ЮТР_ПользователиСлужебный.ЭтоСпециалистОтиз());
		
КонецПроцедуры

&НаСервере
Процедура ЮТР_ПриЧтенииНаСервере(ТекущийОбъект)
	
	УстановитьВыполнениеПослеОбработчиковСобытия("ЮТР_ПриЧтенииНаСервереПосле", Неопределено);		

КонецПроцедуры

&НаСервере
Процедура ЮТР_ПриЧтенииНаСервереПосле(ТекущийОбъект, ДополнительныйПараметр)
	
	Если (НЕ ЭтаФорма.ТолькоПросмотр) И (НЕ ЮТР_ПользователиСлужебный.ЭтоСпециалистОтиз()) И ТекущийОбъект.ЮТР_ДокументУтвержден Тогда
		ЭтаФорма.ТолькоПросмотр = Истина;
	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Процедура ЮТР_ПроизводственнаяЕдиницаПриИзменении(Элемент)
	
	ЮТР_ПроизводственнаяЕдиницаПриИзмененииНаСервере();
	
КонецПроцедуры

&НаСервере
Процедура ЮТР_ПроизводственнаяЕдиницаПриИзмененииНаСервере()
	
	ОчиститьДанныеТабеля();
	
КонецПроцедуры
